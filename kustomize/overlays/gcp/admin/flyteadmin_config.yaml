logger:
  show-source: true
  level: 5
server:
  httpPort: 8088
  grpcPort: 8089
  security:
    secure: false
    useAuth: false
    allowCors: true
    allowedOrigins:
    - http://flyte.example.com
flyteadmin:
  roleNameKey: "iam.amazonaws.com/role"
  profilerPort: 10254
  metricsScope: "flyte:"
  metadataStoragePrefix:
    - "metadata"
    - "admin"
  testing:
    host: http://flyteadmin
database:
  port: 5432
  username: flyte
  host: cloudsqlproxy
  dbname: flyte
  options: "sslmode=disable"
  passwordPath: /etc/gcp/pwd.txt
remoteData:
  scheme: "gcp"
storage:
  type: stow
  stow:
    kind: google
    config:
      json: ""
      project_id: gcp-flyte-test
      scopes: https://www.googleapis.com/auth/devstorage.read_write
  container: "flyte"
task_resources:
  defaults:
    cpu: 1000m
    memory: 1000Mi
    storage: 5Mi
  limits:
    cpu: 2
    memory: 2G
    storage: 20Mi
task_type_whitelist: {}
domains:
  - id: development
    name: development
  - id: staging
    name: staging
  - id: production
    name: production
cluster_resources:
  templatePath: "/etc/flyte/clusterresource/templates"
  refresh: 5m
