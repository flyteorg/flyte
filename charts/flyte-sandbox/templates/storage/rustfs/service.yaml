{{- if .Values.rustfs.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-rustfs
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "flyte-sandbox.labels" . | nindent 4 }}
spec:
  type: NodePort
  ports:
  - name: rustfs-api
    nodePort: 30002
    port: 9000
    targetPort: rustfs-api
  - name: rustfs-console
    port: 9001
    targetPort: rustfs-console
  selector:
    app.kubernetes.io/name: rustfs
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
