{{- if .Values.flyteplugins_service.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ template "flyteplugins_service.name" . }}
  namespace: {{ template "flyte.namespace" . }}
  labels: {{ include "flyteplugins_service.labels" . | nindent 4 }}
  {{- with .Values.flyteplugins_service.service.annotations }}
  annotations: {{ tpl (toYaml .) $ | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.flyteplugins_service.service.type}}
  type: {{ . }}
  {{- end }}
  ports:
  - name: plugin-grpc
    port: 80
    protocol: TCP
    targetPort: plugin-grpc
  selector: {{ include "flyteplugins_service.selectorLabels" . | nindent 4 }}
{{- end }}
