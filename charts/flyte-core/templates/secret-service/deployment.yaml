{{- if .Values.secretService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "flyte-core.componentFullname" (dict "ctx" $ "component" "secret") }}
  namespace: {{ .Release.Namespace }}
  labels: {{- include "flyte-core.componentLabels" (dict "ctx" $ "component" "secret") | nindent 4 }}
spec:
  replicas: {{ .Values.secretService.replicaCount }}
  selector:
    matchLabels: {{- include "flyte-core.selectorLabels" (dict "ctx" $ "component" "secret") | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/secret-service/configmap.yaml") . | sha256sum }}
        {{- with .Values.secretService.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels: {{- include "flyte-core.selectorLabels" (dict "ctx" $ "component" "secret") | nindent 8 }}
        {{- with .Values.secretService.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "flyte-core.serviceAccountName" . }}
      {{- with .Values.secretService.nodeSelector }}
      nodeSelector: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.secretService.tolerations }}
      tolerations: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.secretService.affinity }}
      affinity: {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: secret
          image: "{{ .Values.image.repository }}/secret:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["/usr/local/bin/flyte-secret"]
          args: ["--config", "/etc/flyte/config.d/*.yaml"]
          ports:
            - name: http
              containerPort: {{ .Values.secretService.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 30
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 30
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            {{- with .Values.secretService.extraEnvVars }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          volumeMounts:
            - name: config
              mountPath: /etc/flyte/config.d
          {{- with .Values.secretService.resources }}
          resources: {{- toYaml . | nindent 12 }}
          {{- end }}
      volumes:
        - name: config
          projected:
            sources:
              - configMap:
                  name: {{ include "flyte-core.componentConfigMapName" (dict "ctx" $ "component" "secret") }}
{{- end }}
