{{- if .Values.secretService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "flyte-core.componentFullname" (dict "ctx" $ "component" "secret") }}
  namespace: {{ .Release.Namespace }}
  labels: {{- include "flyte-core.componentLabels" (dict "ctx" $ "component" "secret") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.secretService.port }}
      targetPort: http
      protocol: TCP
  selector: {{- include "flyte-core.selectorLabels" (dict "ctx" $ "component" "secret") | nindent 4 }}
{{- end }}
