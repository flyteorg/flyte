{{- if .Values.events.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "flyte-core.componentFullname" (dict "ctx" $ "component" "events") }}
  namespace: {{ .Release.Namespace }}
  labels: {{- include "flyte-core.componentLabels" (dict "ctx" $ "component" "events") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.events.port }}
      targetPort: http
      protocol: TCP
  selector: {{- include "flyte-core.selectorLabels" (dict "ctx" $ "component" "events") | nindent 4 }}
{{- end }}
