{{- if .Values.runs.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "flyte-core.componentFullname" (dict "ctx" $ "component" "runs") }}
  namespace: {{ .Release.Namespace }}
  labels: {{- include "flyte-core.componentLabels" (dict "ctx" $ "component" "runs") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.runs.port }}
      targetPort: http
      protocol: TCP
  selector: {{- include "flyte-core.selectorLabels" (dict "ctx" $ "component" "runs") | nindent 4 }}
{{- end }}
