{{- if or ( index .Values "flyte-binary" "enabled" ) .Values.sandbox.dev }}
{{- $backendService := "" }}
{{- if index .Values "flyte-binary" "enabled" }}
{{- $backendService = printf "%s-http" ( index .Values "flyte-binary" "fullnameOverride" | default "flyte-binary" ) }}
{{- else }}
{{- $backendService = include "flyte-devbox.localHeadlessService" . }}
{{- end }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "flyte-devbox.fullname" . }}-api
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "flyte-devbox.labels" . | nindent 4 }}
spec:
  rules:
    - http:
        paths:
          {{- range $path := list "/healthz" "/readyz" }}
          - path: {{ $path }}
            pathType: Exact
            backend:
              service:
                name: {{ $backendService }}
                port:
                  number: 8090
          {{- end }}
          - path: /flyteidl2.
            pathType: Prefix
            backend:
              service:
                name: {{ $backendService }}
                port:
                  number: 8090
{{- end }}
---
{{- if index .Values "flyte-binary" "enabled" }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: flyte-console
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "flyte-devbox.labels" . | nindent 4 }}
spec:
  rules:
    - http:
        paths:
          - path: /v2
            pathType: Prefix
            backend:
              service:
                name: {{ printf "%s-console" ( index .Values "flyte-binary" "fullnameOverride" | default "flyte-binary" ) }}
                port:
                  number: 80
{{- end }}
---
{{- if .Values.rustfs.enabled}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rustfs-console
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "flyte-devbox.labels" . | nindent 4 }}
spec:
  rules:
    - http:
        paths:
          - path: /rustfs/console
            pathType: Prefix
            backend:
              service:
                name: rustfs-svc
                port:
                  number: 9001
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rustfs-s3
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "flyte-devbox.labels" . | nindent 4 }}
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: rustfs-svc
                port:
                  number: 9000
{{- end }}
---
