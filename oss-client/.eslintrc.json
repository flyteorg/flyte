{
  "extends": [
    "next/core-web-vitals",
    "next/typescript"
  ],
  "rules": {
    "no-unused-vars": "off",
    "@typescript-eslint/no-unused-vars": [
      "error",
      { "argsIgnorePattern": "^_", "varsIgnorePattern": "^_" }
    ],
    "no-restricted-globals": [
      "error", {
        "name": "window",
        "message": "Don't use `window` directly (SSR). Use getWindow() in windowUtils"
      }
    ]
  }
}
