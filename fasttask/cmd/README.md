# Dummy Fast Task Server

# Fast Task Server
Implement only the Heartbeat API for the Fast Task Server.

ProtoFile: ../proto/fasttask.proto

Using this dummy server, implement a CLI that can act as a server.
When the client connects to the server, send the heartbeat message.

The input should be a json file that contains a list of tasks.
tasks, just have a command to run.


Send all tasks down to the client based on capacity.

Write a input json file in ./testdata/tasks.json